import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};


// here we can use slices  for work like context
const filtersSlice = createSlice({
  name: "filters",
  initialState,
  reducers: {
    // payment states

  },
});

export default filtersSlice;
